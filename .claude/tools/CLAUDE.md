# Optimization helpers

These are explicit local commands, not hooks: errors must exit nonzero. Preserve source
requests/logs, bound reads and output, and never dispatch model calls or shell proxies here.
Graph relationships are partial lexical hints; changes must keep coverage limits visible.
Run `node --test .claude/tools/*.test.js` for edits in this folder. Shared workflow and
project constraints remain in the root instructions; do not duplicate them here.
