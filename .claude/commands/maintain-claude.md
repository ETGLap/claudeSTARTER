# /maintain-claude

Review root + local `CLAUDE.md`, hooks, agents, checklists, context.

- Update only what improves future Claude behavior.
- Keep files low-token; remove duplication.
- Add new files only when a repeated need exists.
