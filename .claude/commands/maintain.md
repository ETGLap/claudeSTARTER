# /maintain

Periodically tend the `.claude/` system and project context.

- Trim reviewers, pipeline, policy, templates: remove duplication, keep files low-token.
- Refresh `.claude/context/project-context.md` (stack, commands, architecture, risks); remove
  outdated facts.
- Add new files only on repeated need; evolve templates when doc conventions drift.
- Keep cross-references valid after any change.
