# /verify-docs

Audit docs and specs against current code (read-only — report, don't write). Find:

- Outdated docs.
- Missing instructions or undocumented areas (api, db, payments, setup, architecture).
- Inconsistencies with code.

Suggest concise updates. Don't rewrite unprompted. To apply changes, use `/docs-update`.
