<!-- Template: a folder-local CLAUDE.md. Generated on demand, never shipped — the directory
     may not exist in a new project.

     Only facts true ONLY here. Before adding a line, apply the category test:
       - identical in every project using this kit  -> .claude/CLAUDE.md
       - true everywhere in this project            -> the root CLAUDE.md
       - true for files matching a glob             -> .claude/rules/<name>.md
       - true only in this directory, no glob fits  -> here
     Anything a glob could express belongs in .claude/rules/ instead: rules ship with the
     kit and work before the directory exists.

     Keep under 40 lines. Do not repeat root or kit rules. -->

# <Folder name>

> One sentence: what lives here and why it is separate.

## Conventions

- <patterns unique to this folder that the code does not make obvious>

## Commands

- <build/test/run commands that differ from the project defaults>

## Risks

- <fragile areas, files not to touch, operations needing confirmation>
