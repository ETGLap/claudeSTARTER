# Setup

> Get the project running locally from a clean checkout.

## Prerequisites

Runtime versions, package manager, system tools.

## Install

```bash
<install command>
```

## Configure

Required env vars (names + what they're for — never real secrets). Where to copy `.env`.

## Run

```bash
<dev / test / build commands>
```

## Verify

How to confirm it's working (URL, health check, expected output).
